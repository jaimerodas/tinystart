# The Dockerfile copies go.mod, go.sum, cmd/ and internal/ by name, so the
# build context barely matters. This just keeps the biggest non-sources out
# of the upload. Do not exclude those four paths here: ignoring one of them
# fails the build outright rather than quietly.
/.git/
/.gitignore

# Ignore all environment files.
/.env*

# The development database.
/storage/

# Ignore Kamal files.
/config/deploy*.yml
/.kamal

# Ignore Docker-related files
/.dockerignore
/Dockerfile*

# The binary a bare `go build` drops at the repo root.
/tinystart
