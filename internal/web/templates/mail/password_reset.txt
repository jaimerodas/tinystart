{{/* The blank line before {{end}} is Rails' mailer.text.erb layout, which is
`<%= yield %>` on a line of its own and so puts a newline after the body. The
parity harness compares the message as sent, whitespace and all. */}}
{{define "password_reset.txt"}}You can reset your password within the next 15 minutes on this password reset page:
{{.URL}}

{{end}}
