<% content_for :title, "New user" %>
<article>
  <h1>Create User</h1>
  <%= render "form", user: @user %>
  <% if @any_users %>
    <div>
      Already registered? <%= link_to "Log in", new_session_path %>
    </div>
  <% end %>
</article>

